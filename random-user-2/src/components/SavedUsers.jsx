import React from 'react'

export default function SavedUsers(userEmail, userFirstName, userAge, userPhone, addToList) {
    
    return (
        <div>
            <button onClick={addToList}>aaa</button>
        </div>
    )
}
